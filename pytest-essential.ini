[pytest]
minversion = 6.0
testpaths = tests
python_files = test_essential_features.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers --strict-config --tb=short
markers =
    integration: marks tests as integration tests (component interactions)
asyncio_mode = auto
